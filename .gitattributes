# Auto-detect text files and normalize them to LF in the repo
* text=auto

# Force specific extensions to LF (useful for scripts/config)
*.py text eol=lf
*.sh text eol=lf
*.md text eol=lf

# If you have Windows-only files, force CRLF:
# *.bat text eol=crlf